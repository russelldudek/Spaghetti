#!/bin/bash
# Ralph loop script for Spaghetti project
# Reads prd.json, finds next story with passes=false, invokes agent (placeholder), updates passes and logs progress
echo "Ralph loop placeholder"
